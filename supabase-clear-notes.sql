-- Clear all messages from the notes table
-- Run this in your Supabase SQL Editor to delete all test messages

DELETE FROM notes;

-- Optional: If you want to see how many messages were deleted first, run:
-- SELECT COUNT(*) FROM notes;

-- After running DELETE, you can verify it's empty with:
-- SELECT * FROM notes;

